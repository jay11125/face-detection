import React from "react";
import Logo from "../Logo/Logo";
import "./Navigation.css";

const Navigation = () => {
	return (
		<div className="container">
			<Logo />
			<h1 className="header">Face Detect App</h1>
		</div>
	);
};

export default Navigation;
